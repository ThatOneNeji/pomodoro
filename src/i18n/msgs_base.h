#pragma once

// Every language file MUST define all of these keys.
// Add new keys here first, then to each language file.
#define MSGS_KEYS \
    X(OPEN)       \
    X(CLOSE)      \
    X(STOP)       \
    X(STATISTICS)
